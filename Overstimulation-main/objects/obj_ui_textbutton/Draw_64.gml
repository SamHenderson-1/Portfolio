/// @description Draws the button's background and text.

// Draw the background.
background_drawer(x - background_margin, y - background_margin, width + (2 * background_margin), height + (2 * background_margin));


// Draw the textbox.
event_inherited();

